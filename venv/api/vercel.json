{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/:1" },
    { "src": "/(.*)", "dest": "/api/:1" }
  ],
  "build": {
    "src": "api/",
    "command": "python ufo.py",
    "functions": { "api/**/*.py": true }
  }
}